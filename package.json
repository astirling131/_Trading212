{
    "name": "trading-212-scraper",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "concurrently -k -n \"Backend,Frontend\" -c \"cyan,magenta\" \"npm run backend\" \"npm run dev --prefix frontend\"",
        "build": "npm run build --prefix frontend",
        "preview": "npm run preview --prefix frontend",
        "backend": "venv\\Scripts\\python -m uvicorn api:app --reload"
    },
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}